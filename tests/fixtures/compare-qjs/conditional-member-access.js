(function () { let proto = { tag: "proto" }; let inherited = Object.create(proto); let own = { tag: "own" }; let nested = { inner: { deep: 3 } }; let pool = [inherited, own]; let rotated = ""; for (let i = 0; i < 4; i++) { rotated += (i % 2 === 0 ? pool[0] : pool[1]).tag; } return (true ? own : own).tag + ":" + (false ? own : own).tag + ":" + (1 ? inherited : own).tag + ":" + (0 ? own : inherited).tag + ":" + (true ? own : own)["tag"] + ":" + (true ? [7, 8] : []).length + ":" + ((true ? own : own).tag === "own") + ":" + rotated + ":" + (true ? nested : nested).inner.deep + ":" + (true ? nested : nested).inner["deep"]; })()
