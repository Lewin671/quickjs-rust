(function () { let p = Promise.resolve(1); let q = p.then(); let log = "a"; Promise.resolve(2).then(function () { log += "b"; }); let invalid = 0; try { Promise.prototype.then.call({}); } catch (error) { invalid += error instanceof TypeError ? 1 : 10; } try { Promise.prototype.then.call(3); } catch (error) { invalid += error instanceof TypeError ? 1 : 10; } return typeof Promise.prototype.then + ":" + Promise.prototype.then.length + ":" + Promise.prototype.propertyIsEnumerable("then") + ":" + (q instanceof Promise) + ":" + (q !== p) + ":" + Object.prototype.toString.call(q) + ":" + log + ":" + invalid; })()
