(function () { let called = ""; let p = new Promise(function (resolve, reject) { called = typeof resolve + ":" + typeof reject; resolve(1); reject(2); }); let resolved = Promise.resolve(3); return typeof Promise + ":" + Promise.length + ":" + (p instanceof Promise) + ":" + Object.prototype.toString.call(p) + ":" + called + ":" + (resolved instanceof Promise) + ":" + (Promise.resolve(resolved) === resolved); })()
