(function () { let fulfilled = Promise.try(function (a, b) { return a + b; }, 2, 3); let rejected = Promise.try(function () { throw 7; }); let handled = rejected.catch(function () {}); let chained = fulfilled.then(function (value) { return value + 1; }); return typeof Promise.try + ":" + Promise.try.length + ":" + Promise.propertyIsEnumerable("try") + ":" + (fulfilled instanceof Promise) + ":" + (rejected instanceof Promise) + ":" + (handled instanceof Promise) + ":" + (chained instanceof Promise); })()
