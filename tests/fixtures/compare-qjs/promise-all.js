(function () { let empty = Promise.all([]); let mixed = Promise.all([Promise.resolve(1), 2]); let chained = Promise.all([1]).then(function () {}); let handled = Promise.all([Promise.reject(7)]).catch(function () { return 8; }); return typeof Promise.all + ":" + Promise.all.length + ":" + Promise.propertyIsEnumerable("all") + ":" + (empty instanceof Promise) + ":" + (mixed instanceof Promise) + ":" + (chained instanceof Promise) + ":" + (handled instanceof Promise) + ":" + Object.prototype.toString.call(empty); })()
