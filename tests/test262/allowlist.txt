# Curated Test262 files that the local harness is expected to run.
#
# Keep this list small and feature-scoped. Add entries only when the engine has
# enough support for the test to provide useful signal.
