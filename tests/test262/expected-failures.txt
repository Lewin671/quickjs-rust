# Test262-derived subset cases that are intentionally expected to fail.
#
# Format:
# path/to/test.js # reason
#
# Entries must also appear in allowlist.txt. A listed case may fail without
# failing the subset run, but if it starts passing then test262-subset.sh fails
# and asks you to remove the stale expected-failure entry.
