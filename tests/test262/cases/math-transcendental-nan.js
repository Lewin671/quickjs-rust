// Derived from: test/built-ins/Math/acos/S15.8.2.2_A1.js
if (Math.acos(NaN) === Math.acos(NaN)) {
  throw "expected Math.acos(NaN) to return NaN";
}
if (Math.asin(NaN) === Math.asin(NaN)) {
  throw "expected Math.asin(NaN) to return NaN";
}
if (Math.atan(NaN) === Math.atan(NaN)) {
  throw "expected Math.atan(NaN) to return NaN";
}
if (Math.atan2(NaN, 1) === Math.atan2(NaN, 1)) {
  throw "expected Math.atan2(NaN, 1) to return NaN";
}
if (Math.cos(NaN) === Math.cos(NaN)) {
  throw "expected Math.cos(NaN) to return NaN";
}
if (Math.exp(NaN) === Math.exp(NaN)) {
  throw "expected Math.exp(NaN) to return NaN";
}
if (Math.log(NaN) === Math.log(NaN)) {
  throw "expected Math.log(NaN) to return NaN";
}
if (Math.sin(NaN) === Math.sin(NaN)) {
  throw "expected Math.sin(NaN) to return NaN";
}
if (Math.tan(NaN) === Math.tan(NaN)) {
  throw "expected Math.tan(NaN) to return NaN";
}
